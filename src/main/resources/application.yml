spring:
  application:
    name: s3-to-hdfs-duckdb

kafka:
  bootstrap-servers: localhost:9092
  group-id: s3-to-hdfs-consumer
  topic: etl.ecommerce.orders
  concurrency: 1

s3:
  endpoint: http://localhost:4566
  region: us-east-1
  access-key: test
  secret-key: test
  path-style-access: true

duckdb:
  memory-limit: "4GB"
  temp-directory: /tmp/duckdb_tmp
  threads: 4

hdfs:
  namenode: hdfs://localhost:8020
  output-base-path: /data

batch:
  max-parquet-bytes: 134217728  # 128 MB — flush when buffered Parquet files reach this size
  flush-interval-ms: 300000     # 5 minutes — flush regardless of size

logging:
  config: classpath:log4j2.xml
